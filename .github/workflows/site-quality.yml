name: Site quality

on:
  pull_request:
    paths:
      - "**/*.html"
      - "assets/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - "seo-meta.config.json"
      - "inject-seo-meta.js"
      - "sitemap.xml"
      - "robots.txt"
  push:
    branches: [main]
    paths:
      - "**/*.html"
      - "assets/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - "seo-meta.config.json"
      - "inject-seo-meta.js"
      - "sitemap.xml"
      - "robots.txt"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify SEO generators are idempotent
        run: |
          node inject-seo-meta.js .
          npm run sitemap:generate
          git diff --exit-code

      - name: Static checks and production build
        run: |
          npm test
          npm run build
          python3 scripts/check_site.py dist
          git diff --check

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Browser, responsive, and accessibility matrix
        run: |
          python3 -m http.server 4173 -d dist >/tmp/varzin-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID' EXIT
          for i in {1..20}; do curl -fsS http://127.0.0.1:4173/ >/dev/null && break; sleep 1; done
          npm run test:browser

      - name: Upload browser evidence on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: browser-evidence
          path: test-results/
          if-no-files-found: ignore
