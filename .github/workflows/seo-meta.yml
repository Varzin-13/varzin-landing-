name: Update SEO meta tags

# Runs the injector automatically whenever HTML files change, and commits
# the result back to the branch. Can also be triggered manually from the
# Actions tab (workflow_dispatch) without needing an HTML file change.

on:
  push:
    paths:
      - "**/*.html"
      - "README.md"
      - "VARZIN_preprint_v2.md"
      - "VPE001A_protocol_design.md"
      - "CITATION.cff"
      - "seo-meta.config.json"
      - "inject-seo-meta.js"
      - "research-outputs.json"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - "scripts/generate-sitemap.cjs"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-meta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install document generator
        run: npm ci --ignore-scripts

      - name: Generate research pages and SEO metadata
        run: npm run site:generate

      - name: Commit changes if any
        run: |
          git config user.name "seo-meta-bot"
          git config user.email "actions@github.com"
          git add -A
          git diff --cached --quiet || git commit -m "chore: update SEO meta tags [automated]"
          git push
